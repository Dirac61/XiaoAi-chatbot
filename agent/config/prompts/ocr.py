# -*- coding: utf-8 -*-
"""
图片 / 文件提取用 prompt。
原硬编码位置：
  - agent/main.py L729-L737   （单图 OCR system_prompt）
  - agent/main.py L741 / L746 （单图 user side 固定文本）
  - agent/main.py L864-L877   （多图 OCR system_prompt）
  - agent/main.py L880        （多图 user side 固定文本）
"""

# ==================== 单图 OCR ====================

# 单图提取 system prompt（500 字上限；保持原要求完全一致）
OCR_SINGLE_IMAGE_SYSTEM_PROMPT = """你是一个专业的图片内容分析助手。请全面分析图片中的信息。

要求：
1. 如果图片包含文字，准确识别并提取所有文字内容
2. 如果图片包含图像（照片、图表、图形等），详细描述图像内容
3. 保持文字的原有顺序和排版结构（如表格按行列格式输出）
4. 输出格式为简洁的自然语言文本，不要使用JSON格式
5. 长度控制在500字以内
6. 对于无法识别的部分，用[无法识别]标注"""

# 单图 OCR 传给模型的用户侧固定文本（和 image_urls 一起组合到 content 数组里）
OCR_SINGLE_IMAGE_USER_TEXT = "请全面分析这张图片的内容，包括文字和图像描述"


# ==================== 多图 OCR ====================

# 多图提取 system prompt（1500 字上限；要求每张都以"图N："开头，编号顺序）
OCR_MULTI_IMAGE_SYSTEM_PROMPT = """你是一个专业的图片内容分析助手。请按图片顺序编号分析所有图片。

输出格式要求：
- 每张图片的分析必须以"图N："开头（N为图片序号，从1开始）
- 图1：描述第一张图片的内容
- 图2：描述第二张图片的内容
- 以此类推

分析要求：
1. 如果图片包含文字，准确识别并提取所有文字内容
2. 如果图片包含图像（照片、图表、图形等），详细描述图像内容
3. 保持文字的原有顺序和排版结构
4. 对于无法识别的部分，用[无法识别]标注
5. 长度控制在1500字以内"""

# 多图 OCR 传给模型的用户侧固定文本（会被拼上多个 image_url）
OCR_MULTI_IMAGE_USER_TEXT = "请全面分析以下所有图片的内容，包括文字和图像描述"


# ==================== OCR 通用参数 ====================

# OCR 温度：越低越稳定（避免模型对图片进行"脑补"），原硬编码 0.3
OCR_TEMPERATURE = 0.3

# OCR 是否关闭思考（OCR 输出纯文本，关思考可避免 reasoning 污染提取结果）
OCR_DISABLE_THINKING = True
